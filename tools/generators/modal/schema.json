{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "modal",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The Modal name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
